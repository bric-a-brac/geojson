package ch.echosystem;

public final class BusSierrois
	{
	/*
	public Agency getAgency()
		{
		final var agency = new Agency();

		agency.setId("Bus Sierrois");
		agency.setName("Bus Sierrois");
		agency.setUrl("https://www.bus-sierrois.ch");
		agency.setTimeZone(TimeZone.getTimeZone("Europe/Zurich"));

		return agency;
		}
	*/

	/*
	public static void build() throws IOException
		{
		final var bs = new BusSierrois();

		bs.getAgency();

		final var stop = new Stop();

		stop.setType(Type.STOP);

		final var route = new Route();

		route.setType(Route.Type.BUS);
		}
	*/

	/*
	public static void csv() throws IOException
		{
		final var buffer = new StringBuilder();

		final var agency = new BusSierrois().getAgency();

		CSVFormat.Builder.create().setHeader("sdsd", "sdd", "sdsdsd").build();
		
		try (final var printer = new CSVPrinter(buffer, CSVFormat.DEFAULT))
			{
			printer.print(agency.getId());
			printer.print(agency.getName());
			printer.print(agency.getUrl());
			printer.print(agency.getTimeZone().getDisplayName(false, TimeZone.LONG));
			
			//TimeZone.getTimeZone("")

			printer.print("sdsds");
			printer.print("sdsds\"sds");
			printer.print("sdsdssd");
			}
		
		System.out.println(buffer.toString());
		}
	*/
	}
